# CS2 Switch Crosshair In Match

This project can convert CS2 crosshair sharing codes into cfg files that can be used to change the crosshair in-game.

## How it works
It will first decode the cs2 crosshair sharing code. Then, an output.cfg file is generated, which contains crosshair-related data that the game can recognize.
